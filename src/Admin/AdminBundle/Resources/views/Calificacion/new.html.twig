{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Calificacion creation</h1>

    {{ form(form) }}

        <ul class="record_actions">
    <li>
        <a href="{{ path('calificacion') }}">
            Back to the list
        </a>
    </li>
</ul>
{% endblock %}
