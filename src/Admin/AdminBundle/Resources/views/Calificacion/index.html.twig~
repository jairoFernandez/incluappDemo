{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Calificacion list</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Experiencia</th>
                <th>Calificacion</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('calificacion_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.experiencia }}</td>
                <td>{{ entity.calificacion }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('calificacion_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('calificacion_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('calificacion_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
