{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Experiencias list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Fechacreacion</th>
                <th>Titulo</th>
                <th>Contenido</th>
                <th>Calificacion</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('experiencias_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{% if entity.fechaCreacion %}{{ entity.fechaCreacion|date('Y-m-d H:i:s') }}{% endif %}</td>
                <td>{{ entity.titulo }}</td>
                <td>{{ entity.contenido }}</td>
                <td>{{ entity.calificacion }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('experiencias_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('experiencias_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('experiencias_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
